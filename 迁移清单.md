# 《重生股神》迁移到 Windows 台式机 · 清单与步骤

> 用途：把 Mac 上的开发环境迁到「9700X + 16GB + RTX5060 + 大存储」的 Windows 台式机，
> 并在台式机上用 DeepSeek 继续开发。
> 核心思路：**代码+数据走网盘，工具链 Windows 重装（模板走网盘传）**。

---

## 一、上传网盘的文件清单

### ✅ 必传（做项目包）
| 文件/目录 | 说明 | 大小 |
|----------|------|------|
| `重生股神-项目包.zip` | **自动打包好的**（见「三」），含 godot/ 全部代码+资源+精简数据(260天) + HANDOFF.md + 迁移清单 | ~650MB |
| `~/workbuddy-tools/export_templates.tpz` | Godot 4.7.2 导出模板 1.28GB（**Windows 重下慢/官网坑多，传网盘最划算**，跨平台通用） | 1.28GB |

### ⭕ 可选（按需）
| 文件/目录 | 说明 | 大小 |
|----------|------|------|
| `game_data/game/`（项目根下的 2.3GB 全量数据） | 以后要重新裁剪更大数据包才需要；A 测阶段 260 天精简包够用 | 2.3GB |
| `scripts/`（pick_pool.py / convert_2023_2025.py / shuffle_build.py） | 数据生成脚本，重新造数据才用；已含在项目包 | 小 |
| `~/workbuddy-tools/NotoSansCJKsc-Regular.otf` | 已含在项目包 assets/fonts | 16MB |

### ❌ 不用传（本地生成）
- `.godot/` 缓存、`build/` 产物、`__pycache__`（打包时已排除）
- JDK / Android SDK（**macOS 版不兼容 Windows**，重装）
- 各种测试临时文件 /tmp

---

## 二、Windows 环境搭建步骤（一次性，约 1~2 小时）

### 1. Godot 4.7.2（编辑器 + 运行）
- 下载：`https://godotengine.org/download/windows/` 选 **4.7.2 stable 标准版（非 Mono）**，解压到 `D:\dev\Godot\`
- 验证：双击 `Godot_v4.7.2-stable_win64.exe`，导入 `godot/project.godot`

### 2. 解压项目包
```
D:\dev\重生股神\          ← 解压 重生股神-项目包.zip
├── godot\                ← Godot 项目（打开它）
├── HANDOFF.md            ← ★ 先读！
└── 迁移清单.md
```

### 3. Python（数据脚本用，一般已装）
- 要求 3.10+；`python --version` 确认。需要 pandas/pyarrow 时：`pip install pandas pyarrow`

### 4. JDK 17（安卓打包）
- 清华镜像：`https://mirrors.tuna.tsinghua.edu.cn/Adoptium/17/jdk/x64/windows/` 下载 `OpenJDK17U-jdk_x64_windows_hotspot_17.0.20.1_1.zip`
- 解压到 `D:\dev\jdk-17`，**设置环境变量 JAVA_HOME**（系统属性 → 高级 → 环境变量）

### 5. Android SDK（命令行装）
```powershell
# 下载 commandlinetools-win：https://dl.google.com/android/repository/commandlinetools-win-11076708_latest.zip
# 解压到 D:\dev\android-sdk\cmdline-tools\latest\
D:\dev\android-sdk\cmdline-tools\latest\bin\sdkmanager.bat --sdk_root=D:\dev\android-sdk "platform-tools" "build-tools;34.0.0" "platforms;android-34"
```
- 设置环境变量 `ANDROID_HOME=D:\dev\android-sdk`

### 6. Godot 导出模板（用网盘传的）
- 把 `export_templates.tpz` 放到任意目录，用 7-Zip/Windows 资源管理器解压（tpz 本质是 zip）
- 模板安装目录（Windows）：`%APPDATA%\Godot\export_templates\4.7.2.stable\`
- 把解压出来的 `templates\` 内全部文件（android_debug.apk、version.txt 等）拷进去

### 7. Godot 编辑器配置（Android 路径）
- Godot → 编辑器 → 编辑器设置 → 导出 → Android：
  - Android SDK 路径：`D:\dev\android-sdk`
  - Java SDK 路径：`D:\dev\jdk-17`（JDK 根目录）
  - Debug Keystore：`D:\dev\debug.keystore`（生成见下）
- 生成 keystore（cmd）：
```cmd
D:\dev\jdk-17\bin\keytool -genkeypair -keystore D:\dev\debug.keystore -alias androiddebugkey -keyalg RSA -validity 9999 -storepass android -keypass android -dname "CN=Android Debug,O=Android,C=US"
```

### 8. 验证
```cmd
cd D:\dev\重生股神\godot
D:\dev\Godot\Godot_v4.7.2-stable_win64.exe --headless --path . res://scenes/main.tscn --quit-after 150 --max-fps 60
```
- 输出无 `SCRIPT ERROR` 即环境 OK（首启会跑 1.6s 预处理属正常）
- 安卓打包：按 HANDOFF.md §10（Godot 导出 → python 塞数据 → zipalign/apksigner 签名）

---

## 三、重新打包项目（Mac 上执行，已自动完成）

```bash
cd /Users/malihao/WorkBuddy/股市仿真游戏
# 打包命令（排除 .godot 缓存 / build / 全量数据 / pycache）：
zip -r 重生股神-项目包.zip godot/ scripts/ HANDOFF.md 迁移清单.md \
    -x "godot/.godot/*" "godot/build/*" "scripts/__pycache__/*"
```

---

## 四、交接给台式机 DeepSeek 的提示词模板

把以下内容 + 项目包解压路径发给台式机上的 DeepSeek（R1 等）：

```
你是《重生股神》Godot 项目的接手开发者。
1) 先完整阅读 D:\dev\重生股神\HANDOFF.md（尤其 §7 GDScript 铁律、§8 已知坑）
2) 项目路径：D:\dev\重生股神\godot（Godot 4.7.2 打开）
3) 本次任务目标：<在这里写你的需求，例如：给安卓首启加加载画面 / 把 main.gd 拆模块 / 设计能力系统的数据与UI框架>
4) 约束：
   - 小步改，每步用 --headless 验证零错误（命令见 HANDOFF.md §3）
   - 不违反 §7 任何铁律
   - 数据层改动先读 HANDOFF.md §4
   - 红涨绿跌、竖屏 460×900、字号勿改小
5) 完成自测后输出：改了哪些文件、验证结果、遗留风险
```

---

## 五、两台机器协作建议

| 场景 | 用哪台 |
|------|--------|
| 写代码 / 跑测试（headless） | Windows（快） |
| 数据预处理重跑 / 大打包 | Windows（CPU/存储强） |
| iOS 打包（未来） | Mac（必须） |
| 便携演示 / 出差 | Mac |

- **代码同步**：以网盘/云盘为中间层，或 git 仓库（推荐建私有仓库，双机 push/pull）
- **每局随机数据**：Mac 和 Windows 各自运行互不影响（每局随机种子）
- **存档**：user:// 各自独立，联调恶魔币需各自结算
